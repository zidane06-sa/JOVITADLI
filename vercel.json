{
  "buildCommand": "cd backend && npm install && cd ../vite-frontend-framework && npm install && npm run build",
  "outputDirectory": "vite-frontend-framework/.next",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}

